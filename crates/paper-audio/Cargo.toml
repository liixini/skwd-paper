[package]
name = "paper-audio"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Shared cpal + ffmpeg audio playback for skwd wallpaper renderers"

[dependencies]
anyhow = { workspace = true }
tracing = { workspace = true }
cpal = "0.16"
libloading = "0.8"
ringbuf = "0.5.2"
ffmpeg-the-third = { version = "6", default-features = false, features = ["codec", "format", "software-resampling"] }

[dev-dependencies]
tempfile = { workspace = true }

[lints.rust]
dead_code = "allow"
